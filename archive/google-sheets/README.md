Archived Google Sheets upload artifacts.

These files are retained only for historical reference.
The active upload path for this project is now the AWS-based queue and upload flow implemented through `ViolationManager`, `ViolationApi`, and the related models.
